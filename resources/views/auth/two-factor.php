<section class="auth-shell shell"><div class="auth-card card"><div class="auth-heading"><span class="maintenance-clock" aria-hidden="true">2</span><p class="eyebrow">Administrator protection</p><h1 class="heading-auth">Two-factor check</h1><p class="muted">Enter the six-digit code from the configured authenticator. This privileged session is intentionally short-lived.</p></div><form class="form-stack" method="post" action="<?= $url('auth.two-factor.verify', [], '/two-factor-challenge') ?>"><?= function_exists('csrf_field')?csrf_field():'' ?><label class="field"><span>Authentication code</span><input name="code" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" maxlength="6" required autofocus></label><button class="button button-gold button-wide">Verify code</button><div class="divider">or</div><label class="field"><span>Recovery code</span><input name="recovery_code" autocomplete="one-time-code" maxlength="64"></label><button class="button button-ghost button-wide" name="use_recovery" value="1">Use recovery code</button></form></div></section>
