<section class="auth-shell shell"><div class="auth-card card"><div class="auth-heading"><span class="maintenance-clock" aria-hidden="true">✉</span><p class="eyebrow">One quick check</p><h1 class="heading-auth">Verify your email</h1><p class="muted">We sent a short-lived verification link to <?= !empty($data['masked_email'])?'<strong>'.$esc($data['masked_email']).'</strong>':'the address you provided' ?>. Open it in the same or another browser to finish.</p></div><div class="stack"><div class="callout"><strong>Didn’t receive it?</strong><p class="fine-print">Check spam, wait a moment, and confirm the spelling in account settings. Resends are rate-limited.</p></div><form method="post" action="<?= $url('auth.verify.resend', [], '/email/verification-notification') ?>"><?= function_exists('csrf_field')?csrf_field():'' ?><button class="button button-gold button-wide">Resend verification email</button></form><form method="post" action="<?= $url('auth.logout', [], '/logout') ?>"><?= function_exists('csrf_field')?csrf_field():'' ?><button class="button button-ghost button-wide">Log out</button></form></div></div></section>
