<?php $codes=isset($data['recovery_codes'])&&is_array($data['recovery_codes'])?$data['recovery_codes']:[]; ?>
<section class="auth-shell shell"><div class="auth-card card auth-card-wider"><div class="auth-heading"><span class="maintenance-clock" aria-hidden="true">#</span><p class="eyebrow">Store offline</p><h1 class="heading-auth">Recovery codes</h1><p class="muted">Each code works once if your authenticator is unavailable. Regenerating codes invalidates the old set.</p></div><?php if($codes): ?><div class="callout" id="recovery-code-list"><code class="recovery-code-grid"><?php foreach($codes as $code): ?><span><?= $esc($code) ?></span><?php endforeach; ?></code></div><div class="button-row"><button class="button button-ghost" type="button" data-copy="#recovery-code-list">Copy codes</button><button class="button button-ghost" type="button" data-print>Print</button></div><?php else: ?><div class="notice notice-warning">Codes are shown only immediately after generation. Regenerate after reauthentication if you did not save them.</div><?php endif; ?><form method="post" action="<?= $url('auth.recovery-codes.regenerate', [], '/settings/recovery-codes') ?>" class="form-stack" data-confirm="Regenerate recovery codes and invalidate all existing ones?"><?= function_exists('csrf_field')?csrf_field():'' ?><label class="field"><span>Current password</span><input type="password" name="password" autocomplete="current-password" required></label>
<?php require dirname(__DIR__) . '/partials/reauth-two-factor.php'; ?>
<button class="button button-danger">Regenerate codes</button></form></div></section>
