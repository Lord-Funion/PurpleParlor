<section class="error-page shell"><div class="error-card"><span class="maintenance-clock" aria-hidden="true">☕</span><p class="eyebrow">A brief intermission</p><h1><?= $esc((string)($data['heading']??'We’re fluffing the velvet.')) ?></h1><p class="lede"><?= $esc((string)($data['message']??'The Purple Parlor is having a careful tune-up. No game round is running and your saved progress is safe.')) ?></p><?php if(!empty($data['expected_back'])): ?><p class="tag tag-gold">Expected back: <?= $esc($data['expected_back']) ?></p><?php endif; ?><div class="button-row"><button class="button button-gold" type="button" data-reload>Check again</button><a class="button button-ghost" href="mailto:<?= $esc((string)($app['support_email']??'support@lordfunion.dev')) ?>">Contact support</a></div><p class="fine-print">Request reference: <?= $esc((string)($data['request_id']??'not available')) ?></p></div></section>
