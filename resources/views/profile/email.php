<?php $profileSection='settings'; ?>
<header class="page-hero"><div class="shell page-hero-inner"><p class="eyebrow">Account security</p><h1>Change email address</h1><p class="lede">The current address stays active until the new address is verified.</p></div></header>
<section class="section shell profile-layout"><?php require __DIR__.'/_sidebar.php'; ?><div class="profile-content"><form class="card settings-section" method="post" action="<?= $url('profile.email.update', [], '/settings/email') ?>"><?= function_exists('csrf_field')?csrf_field():'' ?><div><h2>Verify the change</h2><p class="muted">Your current address is <strong><?= $esc((string)($user['email']??'')) ?></strong>. We will send a one-time verification link to the new address and a security notice to the current address.</p></div><?php if(!empty($data['email_change_pending'])): ?><div class="callout"><strong>A change is already pending.</strong><p>Submitting another eligible request invalidates the earlier verification link.</p></div><?php endif; ?><label class="field"><span>New email address</span><input type="email" name="email" maxlength="254" autocomplete="email" required></label><label class="field"><span>Current password</span><input type="password" name="password" autocomplete="current-password" required><small>Confirm your password again even though this page required recent reauthentication.</small></label><?php require dirname(__DIR__) . '/partials/reauth-two-factor.php'; ?><div class="form-actions"><button class="button button-gold">Send verification link</button><a class="button button-ghost" href="<?= $url('settings.index', [], '/settings') ?>">Cancel</a></div></form></div></section>
